<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/riskwidget_background"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="center_vertical"
    android:orientation="vertical"
    android:padding="8dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        >

        <TextView
            android:id="@+id/riskwidget_municipality"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_gravity="top"
            android:contentDescription="Concelho"
            android:textSize="14sp"
            android:alpha="1"
            android:ellipsize="end"
            android:maxLines="1"
            android:layout_marginBottom="4dp"
            />

        <ImageView
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:paddingBottom="2dp"
            android:src="@drawable/ic_virus_white"
            >
        </ImageView>
    </LinearLayout>

    <TextView
        android:id="@+id/riskwidget_risk"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:contentDescription="Risco de Infecção"
        android:textColor="#89000000"
        android:textSize="18sp"
        android:textStyle="bold" />
</LinearLayout>